const NoRelishItems = () => {
    return (
      <>
        <h5>Don't Forget about relish! </h5>
        <button onClick={ () => window.location.reload() }>Reload Relish</button>
      </>
    )
  }

  export default NoRelishItems;